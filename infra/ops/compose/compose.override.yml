services:
  qdrant:
    healthcheck:
      test: ["CMD-SHELL","curl -fsS http://localhost:6333/readyz >/dev/null || curl -fsS http://localhost:6333/collections >/dev/null"]
      interval: 2s
      timeout: 2s
      start_period: 2s
      retries: 15
