name: Phase-2 Engine (tri-state, precedence, scopes)

on:
  pull_request:
  push:
    branches:
      - feat/**
      - main

jobs:
  phase2:
    runs-on: ubuntu-latest
    env:
      ENGINE_FAIL_OPEN: "false"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install test tools
        run: |
          python -m pip install --upgrade pip
          pip install pytest
      - name: Run Phase-2 tests (engine/api/schema)
        run: |
          pytest -q tests/engine tests/api tests/schema
      - name: Schema dry-run CLI (no DB writes)
        run: |
          python -m apps.constitution_engine.schema.init --dry-run
