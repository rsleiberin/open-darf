# === DARF sample environment ===
# Database (Neo4j)
DB_HOST=localhost
DB_PORT=7687
DB_USER=neo4j
# DB_PASSWORD is secret; set via Docker secret or local env
DB_PASSWORD=

# API
API_HOST=0.0.0.0
API_PORT=8000

# LLM Provider
LLM_PROVIDER=none
# LLM_API_KEY is secret; set via Docker secret or local env
LLM_API_KEY=
